# NAZOV PREMENNYCH
	pouzivat Google Python Style Guide:
		- module_name, package_name, ClassName, method_name, ExceptionName, function_name, GLOBAL_CONSTANT_NAME, global_var_name, instance_var_name, function_parameter_name, local_var_name
		
	rozdelenie primarnej a sekundarnej zlozky:
		- po akomkolvek nazve premennej odelit sekundarnu od primarnej klucovym slovom "primary" resp. "secondary" na konci nazvu premennej
		
	pouzivat vsade np.array() nie list
	
	pouzivat vsade radiany nie stupne
# 
